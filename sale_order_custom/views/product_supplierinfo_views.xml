<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_product_supplierinfo_list_editable" model="ir.ui.view">
        <field name="name">product.supplierinfo.list.editable.sale.order.custom</field>
        <field name="model">product.supplierinfo</field>
        <field name="arch" type="xml">
            <list string="Proveedores del Producto" editable="bottom">
                <field name="product_tmpl_id" column_invisible="1"/>
                <field name="product_id" column_invisible="1"/>
                <field name="partner_id"/>
                <field name="price"/>
                <field name="currency_id"/>
            </list>
        </field>
    </record>

</odoo>

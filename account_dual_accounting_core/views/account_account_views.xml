<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_account_form_inherit_dual" model="ir.ui.view">
        <field name="name">account.account.form.inherit.dual</field>
        <field name="model">account.account</field>
        <field name="inherit_id" ref="account.view_account_form"/>
        <field name="arch" type="xml">
            <field name="tag_ids" position="after">
                <field name="secondary_account_id" options="{'no_create': True}"/>
            </field>
        </field>
    </record>

</odoo>

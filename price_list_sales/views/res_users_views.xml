<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_users_form_pricelist_permissions" model="ir.ui.view">
        <field name="name">res.users.form.pricelist.permissions</field>
        <field name="model">res.users</field>
        <field name="inherit_id" ref="base.view_users_form"/>
        <field name="arch" type="xml">
            <xpath expr="//page[@name='preferences']" position="inside">
                <group string="Listas de Precios">
                    <field name="pricelist_see_all" widget="boolean_toggle"/>
                    <field name="allowed_pricelist_ids" widget="many2many_tags"
                        invisible="pricelist_see_all"/>
                </group>
            </xpath>
        </field>
    </record>

</odoo>

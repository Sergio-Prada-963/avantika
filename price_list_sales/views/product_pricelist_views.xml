<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="product_pricelist_view_list_inherit_state" model="ir.ui.view">
        <field name="name">product.pricelist.list.inherit.state</field>
        <field name="model">product.pricelist</field>
        <field name="inherit_id" ref="product.product_pricelist_view_tree"/>
        <field name="arch" type="xml">
            <field name="country_group_ids" position="attributes">
                <attribute name="column_invisible">1</attribute>
            </field>
            <field name="country_group_ids" position="after">
                <field name="state_ids" widget="many2many_tags" placeholder="Todos los departamentos"/>
            </field>
        </field>
    </record>

    <record id="product_pricelist_view_inherit_state" model="ir.ui.view">
        <field name="name">product.pricelist.form.inherit.state</field>
        <field name="model">product.pricelist</field>
        <field name="inherit_id" ref="product.product_pricelist_view"/>
        <field name="arch" type="xml">
            <field name="country_group_ids" position="attributes">
                <attribute name="invisible">1</attribute>
            </field>
            <field name="country_group_ids" position="after">
                <field name="state_ids" widget="many2many_tags" placeholder="Todos los departamentos"/>
            </field>
        </field>
    </record>

</odoo>

<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_move_form_manual_currency_rate" model="ir.ui.view">
        <field name="name">account.move.form.manual.currency.rate</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <field name="invoice_currency_rate" position="after">
                <field name="invoice_currency_rate_is_manual" invisible="1"/>
            </field>
        </field>
    </record>
</odoo>
